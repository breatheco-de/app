---
description: 
globs: 
alwaysApply: true
---
The authentication of the user is managed through the AuthContext. This context provides the basic functions to login, logout and validate the current session.


To get information regarding the authentication and the user we use the useAuth hook, which consumes from the AuthContext.